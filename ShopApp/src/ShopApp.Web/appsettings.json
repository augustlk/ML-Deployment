{
  "ConnectionStrings": {
    // Leave blank here — supply via DATABASE_URL env var or appsettings.Development.json.
    // Supabase direct connection string format:
    //   Host=db.<ref>.supabase.co;Port=5432;Database=postgres;Username=postgres;Password=<pw>;SSL Mode=Require
    // For Vercel / serverless, use the transaction pooler URL (port 6543).
    "ShopDb": ""
  },
  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft": "Warning",
        "Microsoft.EntityFrameworkCore": "Warning"
      }
    }
  },
  "AllowedHosts": "*"
}
